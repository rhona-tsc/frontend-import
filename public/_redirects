# ✅ Old filtered URLs that no longer exist on the new site
# Return a real 404 instead of redirecting them to /act, which can look like a soft 404 to Google.
/bandsforhire                               genres=:genres                            /404  404
/bandsforhire                               tag=:tag                                  /404  404
/bandsforhire/*                             genres=:genres                            /404  404
/bandsforhire/*                             tag=:tag                                  /404  404


# ✅ API proxy
/api/*  https://tsc-backend-v2.onrender.com/api/:splat  200

# 🚫 IMPORTANT: ensure /assets/* never falls through to SPA index.html
# This preserves normal asset serving, but missing assets will 404 (instead of returning index.html).
/assets/*  /assets/:splat  200

# ✅ SPA fallback (keep LAST)
/*      /index.html  200